  <nav class="navbar navbar-default navbar-fixed-top">
    <!--<div class="container-fluid">-->
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#nav-menu">
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <a class="navbar-brand" href="/">PPO 2016</a>
        </div>
      <div class="collapse navbar-collapse" id="nav-menu">
<!--login -->
        <ul class="nav navbar-nav navbar-right">
          <li><a href="#"><%= session[:user_id] ? User.find(session[:user_id]).get_player_full_name : 'Login' %></a>
            <ul class="dropdown-menu">
              <% if session[:user_id] %>

              <li><%= link_to 'Your profile', User.find(session[:user_id]) %></li>
              <li class="nav-divider"></li>
              <li><%= link_to 'Logoff', logout_path, method: :delete %></li>

              <% else %>

                <div style="min-width: 15em; margin: 5px;">
                  <%= render 'session/login' %>
                </div>

              <% end %>
            </ul>
          </li>
        </ul>

      <% if menu_authorize('SuperAdmin') %>

          <ul class="nav navbar-nav navbar-left">
            <li>
              <a href="#">Meta Administration</a>
              <ul class="dropdown-menu">
                <li><%= link_to 'Tables', tables_path %></li>
                <li><%= link_to 'Roles', roles_path %></li>
                <li><%= link_to 'Nationalities', nationalities_path %></li>
              </ul>
            </li>
          </ul>

      <% end %>

      <% if menu_authorize('Admin') %>
        <% Tournament.all.each do |tournament| %>

          <ul class="nav navbar-nav navbar-left">
            <li>
              <a href="#"><%= tournament.name %></a>
              <ul class="dropdown-menu">
                <li><a href="#">Management</a>
                  <ul class="dropdown-menu">
                    <li><%= link_to 'Enter score', new_tournament_match_path(tournament) %></li>
                  </ul>
                </li>
                <li><%= link_to 'Information', tournament %></li>
              </ul>
            </li>
          </ul>

        <% end %>
      <% end %>
      </div>
    <!--</div>-->
  </nav>
